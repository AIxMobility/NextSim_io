#include <string>
#include <iostream>

#include <gookto_io/InputStation.hpp>

InputStation::InputStation(int id, int lane_ref, int link_ref, int pos) : id(id), lane_ref(lane_ref), link_ref(link_ref), pos(pos){};